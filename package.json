{
  "name": "dexon_bbs",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "parcel src/index.html --out-dir dist/",
    "build": "npm run clean && parcel build --no-source-maps src/*.html --out-dir . --public-url .",
    "clean": "rm -rf st* sc* .cache"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/artistic709/DEXON_BBS.git"
  },
  "author": [
    "artistic709",
    "SiriusTsou",
    "jeremy5189",
    "andy0130tw"
  ],
  "license": "MIT",
  "devDependencies": {
    "parcel-bundler": "^1.12.3"
  }
}
