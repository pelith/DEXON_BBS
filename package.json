{
  "name": "dexon_bbs",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "npm run clean && parcel src/*.html --out-dir dist/",
    "build": "npm run clean && parcel build --no-source-maps src/*.html --out-dir . --public-url .  --no-minify",
    "clean": "rm -rf st* sc* .cache dist content*"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/artistic709/DEXON_BBS.git"
  },
  "author": [
    "artistic709",
    "SiriusTsou",
    "jeremy5189",
    "andy0130tw"
  ],
  "license": "MIT",
  "devDependencies": {
    "parcel-bundler": "^1.12.3"
  }
}
